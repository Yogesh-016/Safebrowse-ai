services:
  - type: web
    name: safebrowse-ai
    env: docker
    plan: free
    envVars:
      - key: GROQ_API_KEY
        sync: false   # you'll paste the real key in Render's dashboard, not here
